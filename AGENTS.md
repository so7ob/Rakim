# قواعد العمل الدائمة

## Git وGitHub

- افحص `git status` قبل المهمة وبعدها، ولا تحذف أو تضمّن تغييرات لا تخصها.
- كل تطوير جديد يبدأ بـ GitHub Issue، وله فرع مستقل باسم يتضمن رقمها.
- لا تعمل أو تنفذ commit مباشرة على `develop` أو `main`، ولا تدمج إلى `main` إلا ضمن Release صريحة.
- شغّل الاختبارات المناسبة قبل Push وPR وMerge، واستخدم Conventional Commits واضحة.
- ارفع Issue Branch وافتح PR إلى `develop` تربط Issue وتغلقها.
- يلزم Review ونجاح Required Checks قبل الدمج؛ يمنع الدمج عند فشلها.
- لا ترفع أسرارًا أو `.env` أو credentials أو dumps أو مفاتيح خاصة.
- لا تستخدم `reset --hard` أو `clean` لإزالة عمل غير معروف، ولا تستخدم force push على فرع مشترك.
- لا تعِد إنشاء Workflow بأثر رجعي للكود المدمج سابقًا؛ فهو Baseline قائم.
- أي عمل سابق غير مدمج يجب تسويته عبر Issue وPR قبل اعتباره مكتملًا.
- اتبع التفاصيل في `docs/development/GIT_WORKFLOW.md`.

## المشروع والواجهة

- `_reference/uae-legislation/` مرجع للقراءة فقط؛ أي تجهيز يكون في `tmp/reference-render/`.
- حافظ على رموز وقوالب `docs/ui-reference/`، ولا تخصص الترويسة أو التذييل داخل صفحة منفردة.
- اختبر كل تغيير بصري بلقطات Playwright ومسار `visual:*`.
- لا تنقل شعارًا أو علمًا أو اسم جهة أو محتوى رسميًا إماراتيًا إلى المنتج.
- تأتي بيانات التشريعات والصفحات والتنقل من REST API وMariaDB، ولا تكررها ثابتة في React.
- يبقى المشروع React + Node.js + MariaDB ومتوافقًا مع Debian 12.
- اربط صفحات الإدارة ومكوناتها المشتركة بـ `AdminLayout` و`AdminPageHeader` و`AdminTabs`، ولا تُعد قائمة مسطحة أو صفحة إعدادات طويلة.
- أي إجراء إداري جديد يحتاج صلاحية `resource.action` مفروضة في الخادم؛ إخفاء الزر في React ليس حماية.
- تُدار استثناءات سياسات سير العمل من تبويب السياسة فقط، وتظهر في شاشة المستخدم للقراءة دون محرر ثانٍ.
