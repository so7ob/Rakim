# توحيد واجهة أعداد الجريدة — Issue #62

تشترك أعداد الجريدة وبقية القوائم المرجعية في `ReferenceDataHeader`، بما يشمل العنوان والوصف ومسار التنقل. تحولت بطاقات الأعداد إلى جدول داخل `admin-card` باستخدام الأنماط القائمة: رقم العدد والملاحظات، تاريخ النشر، الناشر، الحالة الإدارية والإجراءات. تتبع الإضافة والتعديل والحالة الفارغة النصوص والأنماط الموجودة في بقية القوائم.

تحقق بتاريخ 2026-09-09 على قاعدة اختبار معزولة:

- بناء الواجهة وفحص TypeScript: ناجحان.
- اختبارات الواجهة: 11 ناجحاً.
- اختبار `super-management.spec.ts`: 4 ناجحة، بما فيها حذف العدد من الصف على أحجام 1440 و1024 و390 و320.
- تحقق Playwright مباشر بعرضي 1440 و390: إنشاء وتعديل عدد بالنوافذ القائمة، عرض بياناته وملاحظاته، تعطيله وإعادة تفعيله وحذفه، واستمرار كل نتيجة بعد إعادة التحميل.

- Prettier وفحص فرق Git: ناجحان.
- `npm run visual:admin`: 144 عرضاً ناجحاً دون أخطاء console/network/HTTP أو تجاوز أفقي.

[الأنواع على الكمبيوتر](types-1440.png) · [الجريدة على الكمبيوتر](gazettes-1440.png) · [الأنواع على الهاتف](types-390.png) · [الجريدة على الهاتف](gazettes-390.png)

لا تغيير في قاعدة البيانات أو الصلاحيات؛ الحالة الإدارية تأتي من حقل `isActive` الموجود في API وتحدث بعد الإجراء.
