# تقرير التحقق — 2026-09-04

البيئة المنفذة: Debian 12، MariaDB 10.11.18، Node 20.20.0 للاختبار المحلي. حد الإنتاج المعلن Node `>=22.13` بسبب متطلبات Nest الحالية، ويوصى بـNode 24 LTS. خط البذر: 20 تشريعًا عامًا، 90 نسخة مادة، 3 مصادر و118 وثيقة بحث مشتقة؛ احتفظت قاعدة العمل أيضًا بمسودة أدخلها المستخدم.

| البوابة                                 | النتيجة الأخيرة                                                                         |
| --------------------------------------- | --------------------------------------------------------------------------------------- |
| `npm run db:migrate`                    | نجح؛ طبقت `Preamble1700000000005` بعد ترحيلات الأساس                                    |
| `npm run seed`                          | نجح: 20 تشريعًا و5 حسابات أدوار و118 وثيقة بحث                                          |
| `npm run lint`                          | ناجح لكل workspaces                                                                     |
| `npm test`                              | ناجح: API ‏9، web ‏2، worker ‏7؛ يشمل الزمن/التزامن/المناعة/التطبيع وصيغ الاستيراد وOCR |
| `npm run build`                         | ناجح للتطبيقات الثلاثة وPDF worker المحلي                                               |
| `npm run reindex`                       | ناجح: 118 وثيقة مع رموز المواضع                                                         |
| `npm run test:e2e`                      | 30 ناجحًا و6 تخطيات مقصودة لاختبارَي الانحدار المخصصين لسطح المكتب؛ 1440/1024/390/320  |
| axe                                     | لا مخالفات WCAG A/AA آلية في الصفحات العامة الحرجة ولوحة الدخول/الإدارة                 |
| اللقطات والاستجابة                      | أربع لقطات أساس؛ لا overflow في الرئيسية وPDF عند الهاتف؛ reflow مكافئ 200% ناجح        |
| `npm run test:performance`              | 50 طلبًا بعد الإحماء: متوسط 10.70ms، p95 ‏17.02ms، أقصى 30.38ms؛ الهدف 2000ms           |
| `npm run backup`                        | نجح إلى `data/backups/20260904T010458Z` بعد حفظ مصدر OCR الفعلي                         |
| `npm run restore:check`                 | كل البصمات ناجحة؛ استعادة قاعدة العمل المعزولة: 21 تشريعًا، 98 نسخة، 4 مصادر            |
| `npm run test:infra`                    | صياغة Nginx ووحدتا systemd ناجحة                                                        |
| `npm audit --omit=dev --offline --json` | صفر ثغرات إنتاجية بعد تثبيت PDF.js المصحح 5.5.207                                       |
| `pdfinfo`                               | PDF 1.4، صفحة واحدة، بلا JavaScript أو تشفير                                            |

قياس الأداء خاص بعينة التطوير ولا يعمم على ملايين المواد. يجب إعادة benchmark على عتاد الإنتاج وحجم بيانات ممثل قبل اعتماد السعة أو إضافة OpenSearch.
